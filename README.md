# POKEMON-TEAMS-BACKEND(NODEJS)  

backend for api services and querying tools.  

## TOOLS.  

* express, dotenv, mongodb
* nodejs

## API requests.

```
/api/v1/pokemons
```
Queries: ?skip={Number}&limit={Number}&type={String}&gen={Number}